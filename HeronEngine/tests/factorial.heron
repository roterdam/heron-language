script Factorial;

var max = 10;

Factorial(n : Int) : Int
{
	if (n <= 1)
		return 1;
	else 
		return n * Factorial(n - 1);
}

Main() 
{
	foreach (i in 0..max)
		Console.WriteLine(Factorial(i));
}