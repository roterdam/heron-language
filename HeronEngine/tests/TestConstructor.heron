module TestAutoCtor
{
    imports
    {
        Console = new Heron.Windows.Console();
    }
	fields
	{
		a : Int = 42;
		b = 42;
		c : Int;
		d;
	}
	methods
	{
		Main()
		{
			c = 42;
			d = 42;
            
            WriteLine("Testing the module constructor");
			WriteLine("42 expected, " + a.ToString());
		    WriteLine("42 expected, " + b.ToString());
			WriteLine("42 expected, " + c.ToString());
			WriteLine("42 expected, " + d.ToString());
			
            new TestClass();
		}
	}
}

class BaseClass 
{
	fields
	{
		a : Int = 42;
		b = 42;
		c : Int;
		d;
	}
	methods
	{
		Constructor()
		{
			c = 42;
			d = 42;
		}
	}    
}

class TestClass
{
    inherits
    {
        BaseClass;
    }
	fields
	{
		a : Int = 42;
		b = 42;
		c : Int;
		d;
	}
	methods
	{
		Constructor()
		{
            base.Constructor();

			c = 42;
			d = 42;

            WriteLine("Testing the derived class constructor");
			Console.WriteLine("42 expected, " + a.ToString());
			Console.WriteLine("42 expected, " + b.ToString());
			Console.WriteLine("42 expected, " + c.ToString());
			Console.WriteLine("42 expected, " + d.ToString());

            WriteLine("Testing the base class constructor");
			Console.WriteLine("42 expected, " + ba.ToString());
			Console.WriteLine("42 expected, " + bb.ToString());
			Console.WriteLine("42 expected, " + bc.ToString());
			Console.WriteLine("42 expected, " + bd.ToString());
		}
	}
}