module TestReduce
{
  inherits
  {
    Heron.Windows.Console;
  }
  methods
  {    
    Main()
    {     
      var input = 1..5000;
      var output;
      for (i=0; i < 200; i++)
        output = reduce (a, b in input) a + b;
      WriteLine(output[0].ToString());
    }
  }
}